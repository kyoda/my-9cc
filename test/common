#include <stdio.h>
#include <stdlib.h>

void assert(int expected, int actual, char *code) {
    if (expected == actual) {
        printf("%s => %d Passed\n", code, actual);
    } else {
        printf("%s: Failed\n", code);
        printf("%s => %d expected, but got: %d\n", code, expected, actual);
        exit(1);
    }
}
